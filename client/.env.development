# Client configuration for local development. Vite exposes only VITE_* names.

# Base URL of the Express API. 5050 rather than 5000 because macOS AirPlay
# Receiver occupies 5000 and silently intercepts the connection.
VITE_API_URL=http://localhost:5050/api
