// Global test setup — runs before every test file.
// Imports @testing-library/jest-dom so custom matchers (toBeInTheDocument,
// toHaveTextContent, etc.) are available in all test suites.
import '@testing-library/jest-dom';
