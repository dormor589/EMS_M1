AI Work Log — Full Stack Exam Management System, Milestone 1
=============================================================
Format: D{NNN} — <ISO timestamp UTC> | Agent: Implementer | Task: <summary>

-------------------------------------------------------------
D001 — 2026-05-23T19:40:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Stack decision: JavaScript (ES2022+), not TypeScript.
  Rationale: spec_brief.txt does not mandate TypeScript, the course project is
  a foundational milestone with no large team type-safety requirement, and the
  OVERLAY default is JavaScript unless a strong project-fit reason exists.
  TypeScript adds build complexity and learning overhead that slows M1 delivery.
  TypeScript remains available as an upgrade path if the lecturer requires it.

  Files added: 29 client files, 4 server files, README.md, ai-work-log.txt
  (3 + layout/placeholder components, Vite scaffold, Express skeleton).
  Commits: 4 conventional commits on dev branch.
  Wall time: ~25 minutes.

  Summary of work:
  - Created Vite+React 19 client scaffold in client/ with full folder layout
    per spec §6 (app/, components/layout|shared/, pages/auth|teacher|student/,
    services/, data/, models/, styles/).
  - Replaced Vite default App.jsx with EMS_M1 landing component.
  - Added react-router-dom for D005 routing.
  - Created Express 5 server skeleton in server/ with /health endpoint
    (returns { status, milestone, time }).
  - Verified: `node src/app.js` + `curl localhost:4000/health` → 200 OK.
  - Added placeholder React components for all 11 pages (auth ×2, teacher ×5,
    student ×4) plus NavigationMenu + MainLayout.
  - Wrote top-level README.md with install/run instructions.
  - Pushed dev branch to origin.
-------------------------------------------------------------
D002 — 2026-05-23T22:53:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: Services 1/2 — ConfigService, LoggerService, StorageService, NotifyService

  Files added: 4 service files + 4 Vitest test files + vitest.config.js
  Commits: 6 conventional commits on dev branch.
  Wall time: ~35 minutes.

  Summary of work:
  - Installed Vitest 4 + @vitest/ui + jsdom; configured vitest.config.js
    (jsdom environment, globals: true, include src/**/__tests__/).
  - ConfigService: central config singleton — getApiMode(), getStorageKeys(),
    getDefaultExamStatus(), getExamStatusOptions(), getRoles(), getQuestionTypes().
    All values as frozen module-level constants (no magic strings elsewhere).
  - LoggerService: level-filtered logger (debug < info < warn < error, default info).
    Sole file permitted to call console.* directly; wraps console.info/warn/error/debug.
  - StorageService: localStorage wrapper (sole file permitted to access localStorage).
    JSON round-trip, scoped clear() removes only ems_* keys, null on missing/malformed.
  - NotifyService: in-memory subscriber pattern; success/error/warning emit
    { type, message, id, timestamp } objects to registered listeners; subscribe()
    returns unsubscribe fn.
  - 51/51 tests PASS (ConfigService: 13, LoggerService: 13, StorageService: 14,
    NotifyService: 11).
  - Grep verified: localStorage calls only in StorageService.js;
    console.* calls only in LoggerService.js.
  - npm run build passes (81ms).
-------------------------------------------------------------
D003 — 2026-05-23T23:07:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: Services 2/2 — MockApiService + seed data + entity models

  Files added: 9 new files (5 models, 1 seedData, 1 MockApiService, 1 services/index.js,
               2 test files); 2 files updated (App.jsx, Question.js error message fix).
  Commits: 6 conventional commits on dev branch.
  Wall time: ~40 minutes.

  Demo credentials (M1 mock auth — password plain-text, no hashing until M2):
    Teacher  — email: teacher@ems.dev    password: password
    Student  — email: student@ems.dev    password: password

  Seed data (hard-coded UUIDs, deterministic):
    Published exam: "Introduction to Web Development" — 2 questions (1 MC, 1 open-text)
    Draft exam:     "Advanced JavaScript Concepts (Draft)" — 1 open-text question
    Submissions:    empty array (students take exam live in D004+)

  Summary of work:
  - models/util.js: generateId() wrapping crypto.randomUUID() with fallback.
  - User, Question, Answer models: required-field + value validation, toJSON/fromJSON.
  - Exam, Submission models: same pattern; questions/answers stored as plain objects.
  - data/seedData.js: getSeedData() returns deterministic plain-object seed.
  - MockApiService: constructor-injected (storage, config, logger); 7 async methods
    (seedIfEmpty, get, getById, post, put, delete, clear); Promises for M2 compat.
  - services/index.js: DI root; re-exports D002 singletons; creates mockApi wired singleton.
  - App.jsx: useEffect → seedIfEmpty on mount; shows 'mock data seeded' once ready.
  - 106/106 tests PASS (51 D002 + 35 model + 22 MockApiService - overlap).
  - Grep verified: localStorage and console.* constraints still clean.
  - npm run build passes (87ms, 24 modules).
-------------------------------------------------------------
-------------------------------------------------------------
D004 — 2026-05-23T23:20:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: AuthService + Login/Register pages + role state + minimal BrowserRouter

  Files added/updated: AuthService.js, LoginPage.jsx (replaced placeholder),
    RegisterPage.jsx (replaced placeholder), routes.jsx (replaced placeholder),
    App.jsx (updated), services/index.js (updated), AuthService.test.js.
  Commits: 6 conventional commits on dev branch.
  Wall time: ~35 minutes.

  M1 auth limitation: passwords stored and compared plain-text.
  bcrypt + JWT deferred to Milestone 2. DO NOT use real credentials.

  Summary of work:
  - AuthService: constructor injection (mockApi, storage, config, logger);
    login() plain-text compare, persist via StorageService ems_current_user;
    register() with field + email format + duplicate + password-length validation;
    logout(), getCurrentUser() (rehydrates User.fromJSON), isAuthenticated(),
    isTeacher(), isStudent().
  - LoginPage: controlled form (email, password), delegates to auth.login(),
    demo creds hint block, error display + notify.error(), navigate('/') on success.
  - RegisterPage: controlled form (name, email, password, role select),
    roles from config.getRoles() — no magic strings in JSX, delegates to
    auth.register(), error display + navigate('/') on success.
  - services/index.js: added auth singleton (new AuthService(mockApi,storage,config,logger)).
  - routes.jsx: Routes for /, /login, /register; inline Landing shows user
    name+role when authed (Logout button), Login/Register links when not.
  - App.jsx: BrowserRouter wrap; seedIfEmpty on mount, then auth.getCurrentUser();
    passes currentUser + onAuthChange to AppRoutes.
  - 126/126 tests PASS (20 new AuthService tests).
  - Grep verified: localStorage + console.* constraints still clean.
  - npm run build passes (111ms, 37 modules).
-------------------------------------------------------------
-------------------------------------------------------------
D005 — 2026-05-23T23:30:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: Role-based MainLayout + NavigationMenu + route guards

  Files added: MainLayout.jsx (replaced placeholder), NavigationMenu.jsx (replaced
    placeholder), ProtectedRoute.jsx, layout.css, NotFoundPage.jsx, routes.jsx
    (replaced D004 minimal scaffold), App.jsx (simplified), test-setup.js,
    vitest.config.js (updated), NavigationMenu.test.jsx, ProtectedRoute.test.jsx.
  Commits: 7 conventional commits on dev branch.
  Wall time: ~40 minutes.

  Summary of work:
  - Installed @testing-library/react + @testing-library/jest-dom + user-event;
    updated vitest.config.js to include .jsx tests and load test-setup.js.
  - MainLayout: NavigationMenu + <main><Outlet /></main> shell.
  - NavigationMenu: re-reads auth.getCurrentUser() on each location change via
    useEffect([pathname]); three visual states: unauthed/teacher/student.
    Teacher links: Dashboard, My Exams, New Exam, Submissions, Logout.
    Student links: Dashboard, Available Exams, Grades, Logout.
  - ProtectedRoute: no user → Navigate /login; wrong role → Navigate to own home;
    matching role → render children.
  - routes.jsx: full tree with all 9 page routes nested under MainLayout;
    LandingRedirect at '/' dispatches by role; NotFoundPage catch-all.
  - App.jsx: simplified to just BrowserRouter + AppRoutes + seed-on-mount;
    no more prop drilling (NavigationMenu owns its auth state).
  - layout.css: dark navy nav bar, centered content area, responsive flex wrap.
  - 150/150 tests PASS (24 new: 14 NavigationMenu + 8 ProtectedRoute + 2 config).
  - Grep verified: localStorage + console.* constraints clean.
  - npm run build passes (83ms, 51 modules).

  Route guard behaviour (verified in tests):
  - /teacher/exams as student → /student
  - /teacher/exams as unauthed → /login
  - / as teacher → /teacher
  - / as student → /student
  - / as unauthed → /login
-------------------------------------------------------------
-------------------------------------------------------------
D006 — 2026-05-23T23:55:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: ExamService + Teacher flow (Dashboard, Exams list, Create, Edit, Submissions stub)

  Files added: ExamService.js, styles/pages.css, ExamService.test.js (38 tests)
  Files replaced: TeacherDashboard.jsx, TeacherExamsPage.jsx, CreateExamPage.jsx,
                  EditExamPage.jsx, SubmissionsPage.jsx
  Files updated: services/index.js (wired examService), main.jsx (import pages.css)
  Commits: 9 conventional commits on dev branch.
  Wall time: ~45 minutes.

  Summary of work:
  - ExamService: full CRUD + Draft→Published→Closed state machine enforced in
    the service. publishExam() rejects any non-Draft; closeExam() rejects any
    non-Published. Invalid transitions throw 'Invalid status transition: X → Y'.
    updateExam() strips status/id/createdAt from partial to enforce dedicated methods.
    Each question gets id + examId stamped on create/update.
  - services/index.js: added examService singleton (new ExamService(mockApi, config, logger)).
  - pages.css: dark/light button variants (.ems-btn), status badges (.ems-badge--draft/
    published/closed), exam table, form layout, question editor, responsive breakpoints.
  - TeacherDashboard: shows total/draft/published/closed/submissions counters derived
    from getExamsByTeacher(); quick-links to Create and View Exams.
  - TeacherExamsPage: table with status badge, duration, question count, and per-row
    actions (Edit always; Publish if Draft; Close if Published; nothing if Closed).
    Publish/Close call ExamService state machine methods; list re-fetches on success.
  - CreateExamPage: form with title/description/duration + dynamic question list.
    QuestionEditor sub-component supports MC (dynamic option list) and open-text.
    Validates title non-empty + ≥1 question before calling examService.createExam().
  - EditExamPage: same form pre-filled from getExamById(). Shows banner for
    Published/Closed exams. updateExam() strips status so edit cannot bypass machine.
  - SubmissionsPage: stub that reads raw mock data from mockApi.get('submissions');
    shows placeholder when empty. D007 replaces with SubmissionService wiring.
  - 188/188 tests PASS (38 new ExamService tests + all 150 prior).
  - Grep verified: localStorage only in StorageService; console.* only in LoggerService.
  - npm run build passes (103ms, 53 modules).
-------------------------------------------------------------
-------------------------------------------------------------
D007 — 2026-05-24T00:10:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: SubmissionService + Student flow + wire teacher SubmissionsPage

  Files added: SubmissionService.js, SubmissionService.test.js (26 tests)
  Files replaced: StudentDashboard.jsx, AvailableExamsPage.jsx, TakeExamPage.jsx,
                  GradesPage.jsx, SubmissionsPage.jsx (teacher, re-wired)
  Files updated: services/index.js (wired submissionService)
  Commits: 9 conventional commits on dev branch.
  Wall time: ~45 minutes.

  Summary of work:
  - SubmissionService: submitExam() enforces Published-only and single-submission-
    per-(examId,studentId) constraints in the service layer. Double-submit throws
    'Already submitted'. gradeSubmission() is an M1 field-update stub. Four query
    methods cover all read patterns (by exam, by student, by pair).
  - services/index.js: wired submissionService singleton (dep on mockApi + examService).
  - StudentDashboard: counters (available/submitted/graded) via concurrent Promise.all
    of getPublishedExams + getSubmissionsByStudent. Derives counts in JS.
  - AvailableExamsPage: CRITICAL — uses getPublishedExams() (status filter in service),
    then hides exams the student already submitted. Draft and Closed exams are never
    shown to students.
  - TakeExamPage: guards entry (Published + not-yet-submitted). MC questions render as
    radio buttons; open-text as textarea. Auto-save draft to StorageService on each
    answer change; cleared on successful submit (Nice-to-Have §5.2 implemented cleanly).
    Warns on unanswered questions without blocking submission.
  - GradesPage: joins submissions with exam titles in JS (Promise.all, no N+1).
    Sorted most-recent-first. Shows grade/feedback or placeholders.
  - SubmissionsPage (teacher): 2-call strategy (getExamsByTeacher + mockApi.get
    submissions), filters and groups in JS. Exam sections sorted by submission count.
  - 214/214 tests PASS (26 new SubmissionService tests + all 188 prior).
  - Grep verified: localStorage only in StorageService (TakeExamPage draft save goes
    via storage singleton — no direct localStorage calls); console.* only in LoggerService.
  - npm run build passes (117ms, 54 modules).
-------------------------------------------------------------
-------------------------------------------------------------
D008 — 2026-05-24T01:00:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: Documentation + diagrams

  Authored docs/explanation.txt (plain text) and 4 diagram files under
  docs/diagrams/. Spec sections cited: 14, 15.1, 15.2, 15.3.
  Note: task was interrupted mid-execution by a quota cap and resumed
  in a second invocation; no work was duplicated or lost.
  Wall time: ~35 minutes (split across two invocations).

  Files added:
    docs/explanation.txt                  (plain text, no Markdown)
    docs/diagrams/component-hierarchy.txt (actual D001-D007 component tree)
    docs/diagrams/class-diagram.puml      (PlantUML, entities + services)
    docs/diagrams/use-case-diagram.puml   (PlantUML, Teacher + Student + Guest)
    docs/diagrams/entities.txt            (ASCII table, field details, state machine)

  Each artifact was committed separately per overlay requirement.
  Total docs: commits on dev branch.
-------------------------------------------------------------
-------------------------------------------------------------
D009 — 2026-05-24T01:45:00Z
  Agent: Implementer (claude-sonnet-4-6)
  Task: Manual QA walkthrough, §12 acceptance criteria cross-check, polish.

  Manual QA checklist (§16): 13/13 PASS.
  Acceptance criteria (§12): 12/12 PASS.
  npm run build: PASS — 54 modules, 150ms, 0 warnings.
  Final test count: 214/214 PASS (11 test files, 1.72s).
  Polish fixes: NONE required — no FAILs found.
  Wall time: ~30 minutes.

  Files added:
    docs/manual_qa_checklist.txt   (§16 + §12 annotated results, plain text)

  Milestone 1 is ready for dev -> main PR.
  Awaiting user approval before Team Lead opens PR.
-------------------------------------------------------------
