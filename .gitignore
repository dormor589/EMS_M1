# Node / Vite
node_modules/
dist/
.vite/
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env
.env
.env.local
.env.*.local

# Coverage / test artifacts
coverage/
.nyc_output/

# Session ids (worker + Team Lead UUIDs — local, not for sharing)
.implementer_session_id
.qa_session_id
.team_lead_session_id

# Worker runtime state (per-dispatch PID and log files)
audits/*.pid
audits/*.log

# Internal team-coordination artifacts (Team Lead / Implementer / QA process state)
# These were the working artifacts of our deployment process — kept local for
# reference but excluded from the public repo so only project deliverables are visible.
MASTER_PLAN.md
*.overlay.md
deploy_requests/
audits/
qa_requests/
clarifications/
findings.jsonl
.team_lead_cursor
.implementer_cursor
.qa_cursor
docs/STATUS.md
docs/RESUME.md
docs/ai-work-log.txt
docs/spec_brief.txt
docs/manual_qa_checklist.txt
